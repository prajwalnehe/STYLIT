// Header component - Simplified (categories moved to Navbar)
// This component can be used for additional header content if needed in the future
const Header = () => {
  return null; // Header content moved to Navbar
};

export default Header;
